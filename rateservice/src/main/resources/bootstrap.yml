spring:
    application:
        name: rateservice
    profiles:
        active: dev, test, prod
    cloud:
        config:
            uri: http://localhost:8888

server:
    port: 8070
    
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/

